#include <iostream>

#include <string>

char separar(char a)
{
	char out[100];
	for (int i=100;i--) 
		{
		out[i] = in[i];
		}	
		return out[i];
}
int main()
{
  
    char t[100];
    char d[100];  
    
    printf("Digite el numero: \n");
    scanf("%s",&t);
    d=separar(t);
    printf("%s: \n",d);
	  return 0;   
}

